[build]
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ALWAYS"

# Required Railway env vars (set via Railway dashboard):
#   ACCESS_TOKEN   - GitHub PAT with repo scope (drepscore account)
#   REPO_URL       - https://github.com/drepscore/drepscore-app
#   RUNNER_NAME    - civica-railway-1
#   RUNNER_SCOPE   - repo
#   LABELS         - self-hosted,linux,x64
