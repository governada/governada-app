/**
 * GlobeEdges — Edge rendering for the constellation visualization.
 *
 * Previous edge types (NetworkEdgeLines, ConstellationEdges, NeuralMesh)
 * were removed as part of the constellation renaissance — they were decorative
 * connections with no meaningful spatial relationship to alignment data.
 *
 * This file will be rewritten with intra-cluster constellation lines (MST-based)
 * that create Orion-like connect-the-dots patterns within each governance cluster.
 */

// Placeholder — constellation lines will be implemented in a subsequent PR.
